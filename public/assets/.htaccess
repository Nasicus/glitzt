<IfModule mod_headers.c>
  # Everything under /assets/ is content-hashed (Vite output) or GUID-named
  # (user-uploaded litzes). Both are safe to cache forever.
  Header set Cache-Control "public, max-age=31536000, immutable"
</IfModule>
